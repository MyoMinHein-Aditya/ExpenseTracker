# TODO - Choose Date add to transactions

- [x] Update backend: accept `date` in `/api/add-expense` and store it in `created_at` (YYYY-MM-DD) instead of `now`.
- [x] Update backend: accept `date` in `/api/add-money` and store it in `created_at`.
- [x] Update frontend: add date input to Add Expense row and send it in request.
- [x] Update frontend: add date input to Add Money modal and send it in request.
- [ ] Test manually: add entries for past dates, switch month tabs, confirm transactions appear in correct month and balance updates.


